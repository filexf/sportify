<div>
  <div>
    <%= cl_image_tag event.sport.photo.key, height: 300, width: 300, crop: :thumb %>
    Encore <%= event.sport.number_of_players_max - event.participations.count %> joueurs
  </div>
  <div>
    <%= event.name %>
    <%= event.location.address %>
    <%= event.start_at %>
    <%= event.end_at %>
    <%= link_to "Détails", event_path(event) %>
  </div>
</div>
