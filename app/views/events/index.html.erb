<div>
  <%= render "search_bar" %>
</div>


  <% if @search_message %>
    <p>
      <%= @search_message %>
      </p>
    <h2>
      Nos évènements
    </h2>
    <div class="horizontal-scroll-wrapper card-grid my-3">
      <% @events.each do |event| %>
        <%= render "event_card", event:event  %>
      <% end %>
    </div>
  <% else %>
    <h2>
      Pour vous
    </h2>
      <div class="horizontal-scroll-wrapper card-grid my-3">
        <% @events.each do |event| %>
          <%= render "event_card", event:event  %>
        <% end %>
      </div>

  <% end %>
